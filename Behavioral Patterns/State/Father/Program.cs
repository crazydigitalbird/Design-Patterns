namespace Father
{
    class Program
    {
        static void Main(string[] args)
        {
            Father father = new Father();
            father.FindeOut(Mark.Five);
            father.FindeOut(Mark.Five);
            father.FindeOut(Mark.Two);
            father.FindeOut(Mark.Two);
            father.FindeOut(Mark.Two);
            father.FindeOut(Mark.Five);
            father.FindeOut(Mark.Five);
            father.FindeOut(Mark.Two);
        }
    }
}
